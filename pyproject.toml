[project]
name = "streamlit-basic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jmk", email = "jmfighter3@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "authlib>=1.8.0",
    "openai>=3.14.1",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "streamlit[auth]>=1.64.0",
]

[project.scripts]
streamlit-basic = "streamlit_basic:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
